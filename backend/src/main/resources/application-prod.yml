host: "192.67.204.233"

security:
  cors:
    allowed-origin: "${server.protocol}://${server.host}"

websocket:
  stomp:
    allowed-origin: "${server.protocol}://${server.host}"
  game:
    allowed-origin: "${server.protocol}://${server.host}"